package exceptions;

/**
 * The API TypeOne.
 *  a class for the TypeOne exception that extends the Exception.
 * @author oop
 */
public class TypeOne extends Exception{

    /**
     * Constructs a new TypeOne object with the error message.
     *
     * @param errorMessage the error message that is to be printed
     */
    public TypeOne(String errorMessage){
        super(errorMessage);
    }
}
