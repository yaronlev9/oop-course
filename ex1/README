yaronlev9


File description:

1.Book.java: this file contains a class that represents a book, which has a title, author, year of publication
and different literary aspects it contains methods for the object of the book.
2.Library.java: this file contains a class that represents a library, which hold a collection of books. Patrons can
register at the library to be able to check out books. it contains methods for the object of the library.
3.Patron.java: this file contains a class that represents a library patron that has a name and assigns values to
different literary aspects of books capacity.

Design:

1. in the class Library i added a method isPatronReachMax that recieves a patron id and checks if the patron reached
his maximum number of allowed books he can borrow, returns true if yes otherwise returns false.